# Research

## Findings

- `phenotype-ops-mcp-fix` is a Phenotype fork of `nanovms/ops-mcp`.
- The README describes it as an MCP server bridge between agent tools and the
  Phenotype nanoVMs client.
- The checkout was clean and had no existing `sladge` badge.

## Decision

Apply the badge directly to the root README.
